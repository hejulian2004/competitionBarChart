# 2026-07-21

## 移除时间节点解说弹幕中的"节点 · YEAR"标题文字

- 移除了时间节点解说弹幕卡片上方显示的"● 节点 · 2020"标题文字，只保留解说内容本身
- 同时调整了解说文字的位置（垂直居中），减少顶部空白
- 修改了 4 处代码位置（SVG 路径和 Canvas 路径各 2 处），分别位于：
  - `main-core.js`（主源文件）
  - `bar_chart_race_external_labels_no_overflow.html`（合并版 HTML）

## 修复导出（视频/GIF）与预览的显示差异

发现文件中有**两个独立的弹幕渲染系统**（System A 和 System B），之前只修复了预览 SVG 路径，遗漏了：

### System A（~line 7915~8280，原始导出渲染 - 实际被 System B 覆盖）
- **`renderSvgDanmaku`** (line 8074): SVG 渲染中的 dot + 标签文字已移除
- **`getCachedDanmakuContent`** (line 8202): Canvas 渲染中的 dot + 标签文字已移除

### System B（~line 8583~8970，集成版渲染 - 实际生效）
- **`renderSvgDanmaku`** (line 8786): SVG 渲染中的 dot + 标签文字已移除
- **`getCachedContent`** (line 8885): Canvas 渲染中的 dot + 标签文字已移除
- 所有移除位置同步调整了解说文字的 Y 坐标（垂直居中）

## 修复导出与预览的渲染不一致（第三次修复）

经过全面对比 System B（实际使用的渲染系统），发现以下差异并修复：

### 发现的问题及修复
1. **描边颜色不一致**：预览用 `#dbeafe`，导出用 `#bfdbfe` → 全部统一为 `#dbeafe`（4处）
2. **卡片宽度不一致**：预览为全宽（WIDTH - 32），导出为窄卡居中（max 720px） → 改为全宽左对齐
3. **卡片高度含冗余**：导出卡片高度仍包含已移除的 `labelArea + labelGap` → 改为 `padding*2 + lineCount*lineHeight`

### 代码结构
- System A（第一个 IIFE, ~line 7679~8460）：定义 `renderFrame`、`drawDirectCanvasVideoFrame` 等
- System B（第二个 IIFE, ~line 8464~9058）：加载 main-core.js 后覆盖 `renderFrame` 和 `drawDirectCanvasVideoFrame`
- 实际预览流程：勾掉 checkbox → 渲染主图（无弹幕）→ System B 的 SVG overlay 画弹幕
- 实际导出流程：同上 → System B 的 Canvas draw 画弹幕（共用 `getDanmakuLayout`）

## 将"图表宽度缩放"改为 0.1x~1.0x 步进 0.1 的缩放倍率

- HTML 滑块：`min="1" max="10" step="1" value="10"`（整数 1~10，避免浮点精度问题）
- `getChartWidthScale()`：`raw / 10` 转回 0.1~1.0，范围校验 1~10
- `getSelectedScale()`（System A）：`raw / 10` 后 `clamp(0.1, 1.0)`

## 视频/GIF 导出也应用缩放倍率

- `getVideoDimensions()` patched：基础分辨率 × `getSelectedScale()`，对齐偶数
- `getGifDimensions()` patched：基础 GIF 尺寸 × `getSelectedScale()`，对齐偶数

